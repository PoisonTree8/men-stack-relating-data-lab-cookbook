<!DOCTYPE html>
<html lang="en">
  <head><meta charset="UTF-8"><title>Edit Food</title></head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit Food</h1>

    <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT" method="POST">
      <label>Name</label>
      <input type="text" name="name" value="<%= food.name %>" required>
      <button type="submit">Save</button>
    </form>

    <p><a href="/users/<%= user._id %>/foods">Back</a></p>
  </body>
</html>